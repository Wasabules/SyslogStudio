# Served from /SyslogStudio/robots.txt, which crawlers do NOT read: they fetch
# it from the domain root only. It is kept for the day this site moves to one.
#
# What actually keeps the demo out of an index is the noindex meta tag the demo
# build injects into its own page — see frontend/vite.demo.config.js.
User-agent: *
Allow: /

Disallow: /SyslogStudio/demo/
